[project]
name = "orchestrator"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0.1",
  "openai>=1.0.0",
  "python-dotenv>=1.0.1",
]

[project.scripts]
orchestrator = "orchestrator.main:main"
